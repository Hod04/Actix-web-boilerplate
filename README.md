# Actix-web boilerplate

A for building web apps with actix-web and tokio-postgress in Rust

## Prerequisites

- Rust
- Docker
- docker-compose

## Usage

```
docker-compose up -d
cargo run --release
```
